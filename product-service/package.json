{
  "name": "product-service",
  "version": "0.1.0",
  "scripts": {
    "deploy": "cdk deploy",
    "diff": "cdk diff",
    "destroy": "cdk destroy",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "check": "prettier --check .",
    "format": "prettier --write ."
  }
}
